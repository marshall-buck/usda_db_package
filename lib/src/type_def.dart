/// DescriptionRecord (description, description.length, food.id)
///
/// [description] is the description of the food.
/// [description.length] is the length of the description.
/// [food.id] is the id of the food.
typedef DescriptionRecord = (String, int, int);
